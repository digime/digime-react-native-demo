<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.digi.androiddemo">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data android:name="me.digi.sdk.Contracts" android:value="@string/contract_id" />
        <meta-data android:name="me.digi.sdk.AppId" android:value="@string/app_id" />
        <meta-data android:name="me.digi.sdk.AppName" android:value="@string/app_name" />
        <meta-data android:name="me.digi.sdk.Keys" android:value="@string/p12_filename" />
        <meta-data android:name="me.digi.sdk.KeysPassphrase" android:value="@string/p12_pass" />
    </application>
</manifest>